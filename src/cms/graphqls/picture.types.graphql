type Query {
    findPicture(id: Int): findPictureResult
}

type findPictureResult {
    code: Int
    message: String
    data: Pictures
}

type Pictures {
    id: Int
    title: String
    name: String
    Url: String
    sequence: Int
}